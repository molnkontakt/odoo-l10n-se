# account_invoice_reminder_ekopost

Glue module (auto-installed when both `account_invoice_reminder` and the sending
module are present): adds the delivery channel to payment reminders and picks it
as the default for customers without e-mail. See the two parent modules.
